import { cleanProp } from 'frontend/helpers/clean-prop';
import { module, test } from 'qunit';

module('Unit | Helper | clean prop');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cleanProp([42]);
  assert.ok(result);
});
